# i18n-baseline/de.same-as-en.txt
#
# Keys where locales/de.json is byte-identical to core's English value ON
# PURPOSE -- proper nouns, brand names, and words that are genuinely the
# same in German (e.g. "Online", "GitHub"), not a forgotten translation.
#
# scripts/check-key-drift.sh fails on any de.json value that is
# byte-identical to core's English value UNLESS the key is listed here --
# that is what catches the "translated" key that is actually still the
# verbatim, never-translated English string (a key present with an
# untranslated value is worse than a missing key: core's T() renders it
# unconditionally, a missing key at least falls back visibly through
# ADR-0010's chain).
#
# Same staleness rule as the baseline: an entry that is no longer
# byte-identical to core (translated since, or core changed its string) or
# whose key no longer exists at all must FAIL so it gets pruned here --
# this file can only grow through a deliberate, reviewed edit, same as the
# baseline; it is not a shrink-only ratchet either.
#
# One key per line, sorted, unique -- enforced by check-key-drift.sh.
# Regenerate with:
#   scripts/check-key-drift.sh --update-allowlist
app.name
audit.col.details
audit.system_actor
auth.setup.pin
catalog.barcode
catalog.barcodes
catalog.col.name
countrysettings.col.symbol
designer.code
designer.receipt.logo_title
fiscalregister.col.status
help.plugins
import.export_title
import.status
issuereport.my_reports.status
issuereport.screenshot_label
journal.col.sync
journal.detail.offline
journal.detail.online
journal.title
kitchenstations.col.name
kitchenstations.col.status
locations.col.name
locations.col.status
nav.designer
nav.github
nav.journal
nav.plugins
orders.col.status
plugins.import.name
plugins.import.version
plugins.manage.title
plugins.manage.update_available
plugins.tier.community
plugins.trust.official
plugins.type.export
plugins.type.hardware
plugins.type.import
plugins.type.integration
plugins.type.popup
promotions.col.code
promotions.col.status
registers.col.name
registers.col.status
settings.backup.title
settings.display.window_mode_kiosk
settings.retention.export_format_label
setup.country.ir
setup.country.pk
status.offline
status.online
tables.col.status
taxcodes.col.name
tender.scan.barcode
tills.col_name
users.col.pin
users.col.status
