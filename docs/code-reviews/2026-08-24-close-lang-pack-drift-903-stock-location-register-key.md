# lang-pack-drift red — 1 missing key (permissions.action.stock_location_management) — ut-docs#903

**Reviewer**: self-reviewed (mechanical translation-sync follow-up to an
already-independently-reviewed core change, matching the standing pattern
for this check — see closed precedents #925, #862, #910, #746, #891,
#665, #612, #494, #374, #441, #579, #296).
**Branch**: `sync/903-stock-location-register-permission-keys` (base: `main`).
**Date**: 2026-08-24.

## What shipped

`lang-pack-drift` went red on `universal-till`'s `main` (push run
`32713522564`) after merging universal-till#481 (ut-docs#903, a dedicated
`stock_location_management` permission action, reviewed and merged in the
same pipeline cycle), which added one new core key to
`web/locales/en.json`: `permissions.action.stock_location_management`
("Locations & registers").

This was **not** caught in advisory mode on the PR itself — the PR's own
`lang-pack-drift` job is advisory-only by design (per `universal-till/
CLAUDE.md`), and turned blocking only once merged to `main`.

This PR adds the German translation ("Lagerorte & Kassen") to
`locales/de.json`, in alphabetical position among the existing
`permissions.action.*` keys (this file's established ordering), keeping
this pack's existing exact-parity coverage (0-entry
`i18n-baseline/de.untranslated.txt`). No baseline edit needed — the key
previously didn't exist in core, so this is a pure addition.

A companion PR in `ut-plugin-language-es` carries the matching Spanish
translation — both are required to fully close the drift; neither alone
closes it.

## Verified beyond automated tests

- `python3 -c "import json; json.load(open('locales/de.json'))"` — valid JSON.
- `bash scripts/validate.sh` — `ok com.universaltill.language-de v1.1.10 (de)`.
- `UT_CORE_EN_JSON=<local checkout of universal-till's merged en.json,
  commit c482c0a> bash scripts/check-key-drift.sh` — `ok -- 1580/1580 core
  keys translated, 0 known-untranslated (baseline), 57 known-same-as-
  English (allowlist), 0 drift, 0 orphans, 0 empty values, 0
  untranslated-present, 0 token mismatches`.
- The new value carries no `%s`/format-verb placeholder, so no token-count
  cross-check applies beyond the guard's own 0-mismatch report.

## Outcome

Merging via this PR closes `universal-till`'s `lang-pack-drift` red on
`main` once combined with the companion `ut-plugin-language-es` PR. Both
verified green against the merged core `en.json` before push.

---
_Generated by [Claude Code](https://claude.ai/code)_
