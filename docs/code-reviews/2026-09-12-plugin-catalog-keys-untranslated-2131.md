# Code review: record ut-docs#2131's 2 new `plugins.manage.*` keys as known-untranslated

**Date:** 2026-09-12
**Branch:** `chore/2131-plugin-catalog-keys-untranslated`
**Implementer:** Sonnet (inline, cloud lane `lane:cloud-54`)
**Reviewer:** Sonnet (fresh-context subagent, independent of the implementation — `complexity:easy` review tier, mechanical script-generated diff)

## What changed

`universal-till`'s ut-docs#2131 fix (PR #1100, merged) added two new
`en.json` keys — `plugins.manage.update_unknown` /
`plugins.manage.update_unknown_hint` — and turned this repo's
`lang-pack-drift` push-to-`main` check red on `universal-till` the moment
it merged (advisory-only on the PR itself; blocking on push, per
`universal-till/CLAUDE.md`).

Fixed by recording the gap as known-untranslated debt, exactly the
`ut-docs#2005`/`ut-docs#1962` precedent: `scripts/check-key-drift.sh
--update-baseline` (run against a local checkout of the merged
`universal-till` `web/locales/en.json` via `UT_CORE_EN_JSON`, not a live
fetch — the merge had not yet propagated to `raw.githubusercontent.com`'s
cache at the time this ran), which appended exactly the two new keys to
`i18n-baseline/de.untranslated.txt`, sorted, no other change. No
`--allow-growth` needed — the baseline was already non-empty (28 lines /
6 real entries) from prior tracked gaps.

**No translation was hand-typed.** This repo's translation pipeline uses a
self-hosted NAS model, unreachable from this cloud sandbox — same
constraint as every other `blocked:env` translation gap this pipeline has
hit (ut-docs#1775 and others). `ut-plugin-language-es` has the identical
gap (checked independently in its own repo/PR, `chore/2131-...` there
too) — not `de`-specific this time.

## Verified beyond automated tests

- `bash scripts/check-key-drift.sh` (against the merged core `en.json`):
  `ok -- 2344/2350 core keys translated, 6 known-untranslated (baseline),
  78 known-same-as-English (allowlist), 0 drift, 0 orphans, 0 empty
  values, 0 untranslated-present, 0 token mismatches`.
- `bash scripts/validate.sh`: `ok com.universaltill.language-de v1.1.64
  (de)`.
- `bash scripts/check-key-drift.test.sh`: all cases pass, unchanged
  (`scripts/check-key-drift.sh` itself untouched).
- Confirmed the diff touches only `i18n-baseline/de.untranslated.txt`
  (dev bookkeeping, never bundled by `scripts/package.sh`) — no shipped
  file (`manifest.json`, `locales/*`, `README.md`, `LICENSE`) changed, so
  no version bump is required per this repo's own `CLAUDE.md`.
- Confirmed the two added lines are sorted and match exactly what
  `universal-till`'s `en.json` added (`git diff` on `web/locales/en.json`
  in that repo's own merged PR).

## Safe to merge

Yes — a two-line, tool-generated bookkeeping diff, verified idempotent
(re-running the check now passes) and scoped to exactly the drift it's
meant to record.

---
_Generated by [Claude Code](https://claude.ai/code)_
