# 2026-09-16 — Remaining 3-key drift, full DE parity (ut-docs#2245)

## What shipped

Translated the last 3 core keys not yet covered by this pack
(`plugins.manage.update_unknown`, `plugins.manage.update_unknown_hint`,
`settings.shop_type.service_layout_note`), removed their now-stale
entries from `i18n-baseline/de.untranslated.txt`, and bumped
`manifest.json`'s version `1.1.76` → `1.1.77`.

These 3 keys had been sitting in the baseline as accepted, ratcheted
debt (not new drift) — this closes that debt for real per the pack's
own "never baseline, translate" rule. `lang-pack-drift` now sees exact
parity again (2415/2415 core keys translated, 0 known-untranslated).

Card `ut-docs#2245` originally covered 26 `diagnostics.*`/
`settings.diagnostics.*` keys too, but those had already been landed
by a prior cycle (`ut-plugin-language-de#268`, ut-docs#2264) before
this cycle picked the card up — verified via a fresh
`check-key-drift.sh` run against `origin/main`'s current `en.json`
before starting any translation work, which found only these 3 keys
outstanding, not 26/29 as the card's older comments described.

## Independent review

Sonnet subagent, fresh context (card is `complexity:easy`). Verdict:
**safe to merge, no blockers.**

Findings:
- DE translations judged natural and terminology-consistent against
  existing keys in the same file (`Artikel` matches `nav.items`,
  `Küchenstationen` matches `kitchenstations.title`, `Tische` matches
  `tables.title`, `Ausgeblendete Menükacheln` matches
  `menulayout.title`, `Dienstleistung`/`Dienstleistungen` matches
  `setup.shop_type.service`, `Marketplace-Katalog` correctly keeps
  "Marketplace" untranslated within the `plugins.*` namespace, matching
  that namespace's existing convention).
- The one real issue found in this same review round was on the ES
  side only (see the companion PR/review record in
  `ut-plugin-language-es`) — nothing DE-side needed a fix.

What it verified, beyond reading the diff:
- `scripts/validate.sh` and `scripts/check-key-drift.sh` (with
  `UT_CORE_EN_JSON` pointed at a local `universal-till` checkout) both
  run clean: 2415/2415 keys, 0 drift, 0 orphans, 0 empty values, 0
  untranslated-present, 0 token mismatches.
- `scripts/check-version-bump.sh` confirms the bump and correctly
  attributes it to `locales/de.json` + `manifest.json`.
- JSON parses and remains fully alphabetically sorted (2415 keys) both
  before and after the new entries.
- Diff scope is exactly `locales/de.json`, `manifest.json`,
  `i18n-baseline/de.untranslated.txt` — nothing else touched, and the
  baseline file's header comment block is intact (only the 3 trailing
  key lines removed).
- Commit author is `Farshid Mirza <farsid@taskrunnertech.co.uk>` — not
  an AI identity.

## Verified beyond automated tests

- `scripts/validate.sh`, `scripts/check-key-drift.sh` run locally
  against `origin/main`'s `en.json`, both green, both before and after
  the fix.

## Safe to merge

Yes. Merge via `merge_method: "merge"` (never squash/rebase — see
`reviewer` skill's "Merge method" note, ut-docs#250).

## Deferred / out of scope

- Companion PR in `ut-plugin-language-es` covers the Spanish half of
  the same card (same 3 keys, plus one terminology fix from this same
  review round).
- `ut-docs#2245`'s original acceptance criteria mentioned 26 keys total
  (from an earlier scope-check comment); those were already delivered
  by a separate, prior PR (`#268`/ut-docs#2264) before this cycle
  started — not re-verified line-by-line here beyond confirming the
  drift check reports full parity, which implies they're present and
  correctly tracked.

PR: (opened this cycle, see linked ut-docs#2245)
