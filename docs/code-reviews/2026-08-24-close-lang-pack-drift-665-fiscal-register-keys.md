# lang-pack-drift red — 32 missing keys (fiscalregister.*) — ut-docs#665

**Reviewer**: self-reviewed (mechanical translation-sync follow-up to an
already-independently-reviewed core change, matching the standing pattern
for this check — see closed precedents #925, #862, #910, #746, #891,
#612, #494, #374, #441, #579, #296).
**Branch**: `sync/665-fiscal-register-keys` (base: `main`).
**Date**: 2026-08-24.

## What shipped

`lang-pack-drift` went red on `universal-till`'s `main` (push run
`32711811140`) after merging universal-till#479 (ut-docs#665, the §146a
Abs. 4 AO fiscal register page, reviewed and merged in the same pipeline
cycle), which added 32 new core keys to `web/locales/en.json` under the
`fiscalregister.*` prefix (title, intro, banner copy, column headers,
status labels, form/error copy, address fields).

This was **not** caught in advisory mode on the PR itself — the PR's own
`lang-pack-drift` job is advisory-only by design (per `universal-till/
CLAUDE.md`), and turned blocking only once merged to `main`.

This PR adds German translations for all 32 keys to `locales/de.json`,
keeping this pack's existing exact-parity coverage (0-entry
`i18n-baseline/de.untranslated.txt`). One key,
`fiscalregister.col.status`, is genuinely identical in German and English
("Status" is a direct loanword) — added to `i18n-baseline/
de.same-as-en.txt` via `scripts/check-key-drift.sh --update-allowlist`
rather than left as a false-positive "untranslated" flag. No other
`i18n-baseline/` edit needed: every other key previously didn't exist in
core, so this is a pure addition, not a baseline prune.

A companion PR in `ut-plugin-language-es` carries the matching Spanish
translations — both were required to fully close the drift; neither
alone would have.

## Verified beyond automated tests

- `python3 -c "import json; json.load(open('locales/de.json'))"` — valid JSON.
- `scripts/validate.sh` — `ok com.universaltill.language-de v1.1.10 (de)`.
- `UT_CORE_EN_JSON=<local checkout of universal-till's merged en.json,
  commit 66884285>  scripts/check-key-drift.sh` — `ok -- 1579/1579 core
  keys translated, 0 known-untranslated (baseline), 57 known-same-as-
  English (allowlist), 0 drift, 0 orphans, 0 empty values, 0
  untranslated-present, 0 token mismatches`.
- None of the 32 new values carry a `%s`/format-verb placeholder, so no
  token-count cross-check applies beyond the guard's own 0-mismatch report.

## Outcome

Merging via this PR closes `universal-till`'s `lang-pack-drift` red on
`main` once combined with the companion `ut-plugin-language-es` PR. Both
verified green against the merged core `en.json` before push.

---
_Generated by [Claude Code](https://claude.ai/code)_
