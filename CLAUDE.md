# ut-plugin-language-de — rules

Language pack (ADR-0010): asset-only, runtime none, locales/de.json only.
Keep keys in sync with universal-till web/locales/en.json; validate.sh
enforces JSON validity. Never publish by hand — tag v<version>.
